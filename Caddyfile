sharashkinakontora.shop, www.sharashkinakontora.shop {
    encode gzip zstd

    handle /static/* {
        root * /srv
        file_server
    }

    handle /media/* {
        root * /srv
        file_server
    }

    handle {
        reverse_proxy web:8000
    }
}
